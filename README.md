A basic, configurable RabbitMQConsumer.

Only need to provide a rabbit host and a routing key for the queue you wish to read messages from. 

Launch as such:

dotnet run host="yourhost" queue"yourroutingkey"

